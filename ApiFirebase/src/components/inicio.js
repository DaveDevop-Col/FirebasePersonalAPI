export function mostrarInicio(container) {
  container.innerHTML = `
    <div id="inicio">
      <h1>Bienvenido a Trivia App</h1>
      <p>Explora las categorías, responde preguntas y guarda tus favoritas.</p>
    </div>
  `;
}
